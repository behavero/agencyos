# OnyxOS Architecture

## 📊 System Overview

OnyxOS is a full-stack agency management platform built with:

- **Frontend**: Next.js 16 (App Router) + React 19
- **Backend**: Next.js API Routes + Supabase Edge Functions
- **Database**: PostgreSQL (Supabase)
- **AI**: Groq (Llama 3.3 70B) via Vercel AI SDK
- **Hosting**: Vercel

```
┌──────────────────────────────────────────────────────────┐
│                      CLIENT LAYER                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │
│  │   Browser   │  │  Telegram   │  │  Mobile (PWA)   │   │
│  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘   │
└─────────┼────────────────┼──────────────────┼────────────┘
          │                │                  │
          ▼                ▼                  ▼
┌──────────────────────────────────────────────────────────┐
│                     NEXT.JS APP                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │                   API ROUTES                         │ │
│  │  /api/chat  /api/webhook  /api/cron  /api/auth      │ │
│  └──────────────────────┬──────────────────────────────┘ │
│  ┌─────────────────────┐│┌────────────────────────────┐  │
│  │    ALFRED AI        ││        SERVICES            │  │
│  │  ┌─────────────┐    │││  ┌──────────────────────┐  │  │
│  │  │   Tools     │    │││  │  FanvueClient        │  │  │
│  │  │  - Finance  │    │││  │  KPIEngine           │  │  │
│  │  │  - Social   │    │││  │  PayrollService      │  │  │
│  │  │  - Scraper  │    │││  │  GhostTracker        │  │  │
│  │  └─────────────┘    │││  └──────────────────────┘  │  │
│  └─────────────────────┘│└────────────────────────────┘  │
└─────────────────────────┼────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│                     DATA LAYER                            │
│  ┌──────────────────┐  ┌───────────────────────────────┐ │
│  │    SUPABASE      │  │      EXTERNAL APIs            │ │
│  │  ┌────────────┐  │  │  ┌───────────────────────┐    │ │
│  │  │ PostgreSQL │  │  │  │  Fanvue API           │    │ │
│  │  │  - RLS     │  │  │  │  Meta Graph API       │    │ │
│  │  │  - Cron    │  │  │  │  YouTube API          │    │ │
│  │  │  - Queues  │  │  │  │  Firecrawl            │    │ │
│  │  └────────────┘  │  │  │  Groq AI              │    │ │
│  │  ┌────────────┐  │  │  │  Telegram Bot API     │    │ │
│  │  │  Auth      │  │  │  └───────────────────────┘    │ │
│  │  └────────────┘  │  │                               │ │
│  └──────────────────┘  └───────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

---

## 🗃️ Database Schema

### Core Entities

```
agencies (1) ──< models (N)
    │
    ├──< profiles (users/team)
    │       │
    │       ├──< timesheets
    │       ├──< shifts
    │       └──< model_assignments
    │
    ├──< quests
    ├──< expenses
    ├──< knowledge_base
    ├──< chat_scripts
    ├──< invitations
    └──< bio_pages ──< bio_blocks
```

### Multi-Tenancy

All tables include `agency_id` for multi-tenant isolation. RLS policies ensure users only access their agency's data.

---

## 🔐 Authentication Flow

1. **Login**: User submits credentials
2. **Supabase Auth**: Validates and returns JWT
3. **Middleware**: Checks session on protected routes
4. **RLS**: Database enforces row-level security

```
User → /login → Supabase Auth → JWT → Middleware → Protected Route
                                         │
                                         ▼
                                  RLS Policies (DB)
```

---

## 🤖 Alfred AI Architecture

Alfred is a ReAct Agent using Groq's tool-calling:

```
┌────────────────────────────────────────────────┐
│               USER MESSAGE                      │
│  "How much did we make this week?"             │
└───────────────────────┬────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────┐
│               GROQ LLM                          │
│  Model: llama-3.3-70b-versatile                │
│  ┌────────────────────────────────────────┐    │
│  │  SYSTEM PROMPT                         │    │
│  │  - OnyxOS context                      │    │
│  │  - Available tools                     │    │
│  │  - ReAct instructions                  │    │
│  └────────────────────────────────────────┘    │
└───────────────────────┬────────────────────────┘
                        │
            ┌───────────┴───────────┐
            │   Tool Selection      │
            │   (If needed)         │
            └───────────┬───────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌───────────────┐ ┌───────────┐ ┌─────────────────┐
│get_financials │ │ scrape_web│ │analyze_health   │
└───────┬───────┘ └─────┬─────┘ └────────┬────────┘
        │               │                │
        └───────────────┴────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────┐
│               FINAL RESPONSE                    │
│  "This week you earned $2,340 (+15% vs last)." │
└────────────────────────────────────────────────┘
```

### Available Tools

| Tool                      | Description                 |
| ------------------------- | --------------------------- |
| `get_agency_financials`   | Revenue, expenses, treasury |
| `get_model_stats`         | Model-specific metrics      |
| `analyze_business_health` | KPIs and conversion rates   |
| `get_upcoming_posts`      | Content calendar            |
| `scrape_web`              | Web scraping via Firecrawl  |
| `search_playbook`         | Search knowledge base       |
| `get_payroll_estimate`    | Calculate staff payments    |

---

## 📡 API Routes Structure

```
/api
├── /alfred          # AI chat endpoints
├── /auth            # Authentication
│   ├── /login
│   └── /social      # OAuth flows (YouTube, Meta)
├── /bio             # Bio page management
├── /chat            # Main Alfred chat
├── /content         # Content management
├── /cron            # Scheduled jobs
├── /expenses        # Expense tracking
├── /fans            # Fan management
├── /ghost           # Public profile tracking
├── /invitations     # Team invites
├── /payroll         # Payroll calculations
├── /quests          # Gamification
├── /scripts         # Chat scripts
├── /shifts          # Shift management
├── /timeclock       # Time tracking
├── /webhook         # Fanvue webhooks
└── /webhooks        # Telegram webhook
```

---

## 🔄 Cron Jobs

Scheduled via Supabase `pg_cron`:

| Schedule     | Job               | Description                      |
| ------------ | ----------------- | -------------------------------- |
| Every 15 min | Late Shift Alert  | Ping owner if employee is late   |
| Every hour   | Missed Post Check | Alert for missed scheduled posts |
| Daily 8 AM   | Daily Briefing    | Send Telegram summary            |
| Daily 6 AM   | Fanvue Sync       | Fetch latest revenue data        |

---

## 🎨 UI Component Hierarchy

```
<Layout>
  <Sidebar />
  <Header>
    <TimeClock />
    <UserMenu />
  </Header>
  <Main>
    <Page />  <!-- Dynamic content -->
  </Main>
  <FloatingChat />  <!-- Alfred -->
</Layout>
```

---

## 🚀 Deployment Pipeline

```
Developer Push → GitHub → GitHub Actions (CI)
                              │
                    ┌─────────┴─────────┐
                    │   Lint & Types    │ ← Fails = Block
                    │   Build           │ ← Fails = Block
                    │   Security Audit  │ ← Warning only
                    └─────────┬─────────┘
                              │
                              ▼ (If main branch)
                         Vercel Deploy
                              │
                              ▼
                      Production Live
```

---

## 📈 Monitoring & Analytics

- **Vercel Analytics**: Performance & Core Web Vitals
- **Supabase Dashboard**: Database metrics
- **Custom KPIs**: `kpi-engine.ts` calculates business metrics
- **Error Tracking**: Consider adding Sentry (future)

---

## 🔮 Future Considerations

1. **Mobile App**: React Native with shared API
2. **Real-time Features**: Supabase Realtime subscriptions
3. **Multi-language**: i18n support
4. **White-labeling**: Custom branding per agency
